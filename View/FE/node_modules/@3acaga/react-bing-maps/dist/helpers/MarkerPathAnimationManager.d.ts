import { EntityDescriptor } from "../index";
declare class MarkerPathAnimationManager {
    private isRunning;
    private lvlMin;
    private lvlMax;
    private entities;
    private timeParts;
    constructor(entities: EntityDescriptor[] | Readonly<EntityDescriptor[]>, animationDuration: number);
    private animate;
    start: () => Promise<void>;
    stop(): void;
}
export default MarkerPathAnimationManager;
