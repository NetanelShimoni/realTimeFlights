var addHandlers = function (_a) {
    var target = _a.target, map = _a.map, handlers = _a.handlers;
    // Add handlers to pushpin
    handlers.forEach(function (_a) {
        var eventName = _a.eventName, handler = _a.handler, throttleMs = _a.throttleMs;
        if (handler) {
            var _handler = function (e) {
                handler(e, map);
            };
            if (throttleMs) {
                window.Microsoft.Maps.Events.addThrottledHandler(target, eventName, _handler, throttleMs);
            }
            else {
                window.Microsoft.Maps.Events.addHandler(target, eventName, _handler);
            }
        }
    });
};
export default addHandlers;
